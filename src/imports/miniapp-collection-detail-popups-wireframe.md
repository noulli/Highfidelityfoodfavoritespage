# Collection Detail Page — Popup Components Wireframe

## Popup Index

| # | Component | Trigger | Placement |
|---|---|---|---|
| 5 | Add store form — link step (retro-form-sheet) | tap FAB | bottom |
| 6 | Add store form — detail step (retro-form-sheet) | after link parse success | bottom |
| 7 | Edit store form (retro-form-sheet) | tap edit on store card | bottom |

---

## 5. Add Store Form — Link Step (retro-form-sheet)

First step of the add store flow. User pastes a review link.

```
  ┌──────────────────────────────────┐
  │          ━━━                     │  ← drag handle
  │  ┌──────────────────────────┐    │
  │  │ 添加店铺          [✕]    │    │  ← title + close button
  │  │ ─────────────────────── │    │  ← header divider (showHeaderDivider)
  │  └──────────────────────────┘    │
  │                                  │
  │  ┌──────────────────────────┐    │  ← scroll body
  │  │                          │    │
  │  │  粘贴点评链接             │    │  ← label
  │  │  ┌──────────────────┐    │    │
  │  │  │ 将点评里的餐厅链  │    │    │  ← textarea placeholder
  │  │  │ 接粘贴到这里...   │    │    │
  │  │  │                  │    │    │
  │  │  └──────────────────┘    │    │
  │  │                          │    │
  │  │  {addFieldErrors.rawInput}│   │  ← error text
  │  │  {addPlaceError}          │   │  ← general error
  │  │                          │    │
  │  │  💡 提示：打开点评 App，  │   │  ← tip
  │  │  找到想添加的餐厅，点击   │   │
  │  │  "分享"并复制链接到这里。 │    │
  │  │                          │    │
  │  └──────────────────────────┘    │
  │  ────────────────────────────── │  ← footer divider (showFooterDivider)
  │  [       解析并继续        ]    │  ← submit button
  └──────────────────────────────────┘
```

**Props:** `visible`, `title` = `"添加店铺"`, `showHeaderDivider`, `showFooterDivider`, `submitText` = `"解析并继续"` (or `"提交中..."`), `submitDisabled`, `submitLoading`

**Events:** `visiblechange`, `close`, `submit`

**Data (`addForm`):** `rawInput` (textarea value)

**Error fields:** `addFieldErrors.rawInput`, `addPlaceError`

---

## 6. Add Store Form — Detail Step (retro-form-sheet)

Second step. Shows parsed store info and lets user fill details.

```
  ┌──────────────────────────────────┐
  │          ━━━                     │
  │  ┌──────────────────────────┐    │
  │  │[←] 店铺详情        [✕]  │    │  ← back button + close
  │  │ ─────────────────────── │    │
  │  └──────────────────────────┘    │
  │                                  │
  │  ┌──────────────────────────┐    │
  │  │  ┌─────┐                 │    │
  │  │  │ 📍  │ {addForm.name}  │    │  ← store card preview
  │  │  │     │ {addForm.address│    │
  │  │  └─────┘                 │    │
  │  │                          │    │
  │  │  ── conditional fields ──│    │
  │  │                          │    │
  │  │  店铺名称  (if !name)    │    │  ← input, shown only when name empty
  │  │  ┌──────────────────┐    │    │
  │  │  │ 请输入店铺名称    │    │    │
  │  │  └──────────────────┘    │    │
  │  │                          │    │
  │  │  店铺地址  (if !address) │    │  ← input, shown only when address empty
  │  │  ┌──────────────────┐    │    │
  │  │  │ 请输入店铺地址    │    │    │
  │  │  └──────────────────┘    │    │
  │  │                          │    │
  │  │  推荐理由（可选）        │    │  ← textarea
  │  │  ┌──────────────────┐    │    │
  │  │  │ 补充推荐理由      │    │    │
  │  │  └──────────────────┘    │    │
  │  │                          │    │
  │  │  推荐菜                  │    │
  │  │  ┌────┐ ┌────┐ ┌────┐   │    │  ← dish tags
  │  │  │菜₁✕│ │菜₂✕│ │菜₃✕│   │    │
  │  │  └────┘ └────┘ └────┘   │    │
  │  │                          │    │
  │  │  ┌──────────────────┐    │    │  ← dish editor (when visible)
  │  │  │ 输入菜名   [添加]│    │    │
  │  │  │            [取消]│    │    │
  │  │  └──────────────────┘    │    │
  │  │  ─ or ─                  │    │
  │  │  [＋ 添加菜品]           │    │  ← add dish button (when editor hidden)
  │  │                          │    │
  │  │  {addFieldErrors.placeId}│    │
  │  │  {addPlaceError}         │    │
  │  │                          │    │
  │  └──────────────────────────┘    │
  │  ────────────────────────────── │
  │  [       提交添加          ]    │
  └──────────────────────────────────┘
```

**Props:** `visible`, `title` = `"店铺详情"`, `showBack` = `true`, `backDisabled`, `showHeaderDivider`, `showFooterDivider`, `submitText` = `"提交添加"` (or `"提交中..."`), `submitDisabled`, `submitLoading`

**Events:** `visiblechange`, `close`, `back`, `submit`

**Data (`addForm`):** `name`, `address`, `reason`, `dishes[]`, `dishInput`

**Error fields:** `addFieldErrors.name`, `addFieldErrors.address`, `addFieldErrors.reason`, `addFieldErrors.placeId`, `addPlaceError`

### Dish Tag

```
  ┌─────────┐
  │ {name} ✕ │  ← tag text + remove button
  └─────────┘
```

### Dish Editor

```
  ┌──────────────────────────────┐
  │ 输入菜名              [添加] │  ← input + add button
  │                       [取消] │  ← cancel button
  └──────────────────────────────┘
```

---

## 7. Edit Store Form (retro-form-sheet)

Similar to add detail step, but for editing an existing store.

```
  ┌──────────────────────────────────┐
  │          ━━━                     │
  │  ┌──────────────────────────┐    │
  │  │ 编辑店铺           [✕]  │    │  ← no back button
  │  │ ─────────────────────── │    │
  │  └──────────────────────────┘    │
  │                                  │
  │  ┌──────────────────────────┐    │
  │  │  ┌─────┐                 │    │
  │  │  │ 📍  │ {storeName}     │    │  ← store card preview (read-only)
  │  │  │     │ {storeAddress}  │    │
  │  │  └─────┘                 │    │
  │  │                          │    │
  │  │  {editShopError}         │    │  ← general error
  │  │  {editFieldErrors.storeId}│   │
  │  │                          │    │
  │  │  推荐理由                 │    │  ← textarea
  │  │  ┌──────────────────┐    │    │
  │  │  │ 更新推荐理由      │    │    │  ← placeholder
  │  │  └──────────────────┘    │    │
  │  │                          │    │
  │  │  推荐菜                  │    │
  │  │  ┌────┐ ┌────┐ ┌────┐   │    │  ← dish tags
  │  │  │菜₁✕│ │菜₂✕│ │菜₃✕│   │    │
  │  │  └────┘ └────┘ └────┘   │    │
  │  │                          │    │
  │  │  [＋ 添加菜品]           │    │  ← add dish button
  │  │                          │    │
  │  └──────────────────────────┘    │
  │  ────────────────────────────── │
  │  [       提交编辑          ]    │
  └──────────────────────────────────┘
```

**Props:** `visible`, `title` = `"编辑店铺"`, `showHeaderDivider`, `showFooterDivider`, `submitText` = `"提交编辑"` (or `"提交中..."`), `submitDisabled`, `submitLoading`

**Events:** `visiblechange`, `close`, `submit`

**Data (`editForm`):** `storeId`, `storeName`, `storeAddressDisplay`, `reason`, `dishes[]`, `dishInput`

**Error fields:** `editShopError`, `editFieldErrors.storeId`, `editFieldErrors.reason`

---

## Shared Sub-Component: Dish Tag List

Used in both add detail step and edit form.

```
  ┌──────────────────────────────────┐
  │  ┌────────┐  ┌────────┐         │
  │  │ 草头圈子 ✕│  │ 油爆虾  ✕│         │  ← existing tags
  │  └────────┘  └────────┘         │
  │                                  │
  │  Editor mode:                    │
  │  ┌──────────────────────────┐    │
  │  │ [输入菜名___________]    │    │
  │  │              [添加] [取消]│    │
  │  └──────────────────────────┘    │
  │                                  │
  │  ─ or (idle mode) ─              │
  │  [ ＋ 添加菜品 ]                │  ← t-icon "add" + text
  └──────────────────────────────────┘
```

**Data fields:** `dishes[]` (string array), `dishInput` (current input), `dishInputVisible` (toggle editor)

**Interactions:**
- Tap tag `✕` → remove dish from array
- Tap "添加菜品" → show dish editor
- Type + confirm → add dish to array, clear input
- Tap "取消" → hide dish editor

---

## Shared Component: retro-action-sheet

Reusable bottom sheet for action lists.

```
  ┌──────────────────────────────────┐
  │  {title}                         │  ← optional title
  │                                  │
  │  ┌────────────────────────────┐  │
  │  │  {item.label}              │  │  ← normal item
  │  ├────────────────────────────┤  │
  │  │  {item.label}              │  │  ← danger item (red text)
  │  └────────────────────────────┘  │
  │                                  │
  │  [取消]                          │  ← cancelText
  └──────────────────────────────────┘
```

**Props:** `visible`, `title`, `items[]` (`{action, label, danger}`), `cancelText`

**Events:** `visiblechange`, `action` (payload: `{action}`), `cancel`

---

## Shared Component: retro-form-sheet

Reusable bottom sheet form container with header, scrollable body, and footer.

```
  ┌──────────────────────────────────┐
  │          ━━━                     │  ← drag handle
  │  ┌──────────────────────────┐    │
  │  │ [←]  {title}       [✕]  │    │  ← optional back + title + close
  │  │     {subtitle}           │    │  ← optional subtitle
  │  │ ─────────────────────── │    │  ← optional header divider
  │  └──────────────────────────┘    │
  │                                  │
  │  ┌──────────────────────────┐    │
  │  │  scroll-view             │    │  ← scrollable body (slot)
  │  │  <slot />                │    │
  │  │                          │    │
  │  └──────────────────────────┘    │
  │  ────────────────────────────── │  ← optional footer divider
  │  [       {submitText}      ]    │  ← t-button primary round
  └──────────────────────────────────┘
```

**Props:** `visible`, `title`, `subtitle`, `showHeaderDivider`, `showFooterDivider`, `showBack`, `backDisabled`, `submitText`, `submitDisabled`, `submitLoading`, `closeDisabled`, `panelBackground`

**Events:** `visiblechange`, `close`, `back`, `submit`
